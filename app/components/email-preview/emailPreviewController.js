define(
    [
        'angular'
    ],
    function (angular) {
        'use strict';

        return function emailPreviewController($scope) {
            var vm = this;

            this.headerText = 'Email preview';
        };
    }
);
